spring:
  application:
    name: badminton-system

  # ===== File Upload =====
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  # ===== Database (MS SQL Server) =====
  # Do NOT put real passwords here!
  # Create your own application-local.yml (see README)
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=BadmintonDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: BadmintonDB2026!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  # ===== JPA / Hibernate =====
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        physical_naming_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        format_sql: true

  # ===== Thymeleaf =====
  thymeleaf:
    cache: false

  # ===== Gmail SMTP (寄送驗證碼) =====
  mail:
    host: smtp.gmail.com
    port: 587
    username: ygtq.badminton@gmail.com
    password: naihwtapqqhfzcip
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

# ===== Custom Upload Directory =====
app:
  upload:
    dir: ./uploads/products

# ===== LINE Pay Sandbox =====
linepay:
  channel-id: "2010086842"
  channel-secret: "547115f3127c7468277b10cb22c22f11"
  api-url: "https://sandbox-api-pay.line.me"
  confirm-url: "http://localhost:5173/payment/linepay/confirm"
  cancel-url: "http://localhost:5173/payment/linepay/cancel"
